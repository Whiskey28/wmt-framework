# 高可用系统架构演进指南

> 从单体应用到高可用系统的完整演进路径（Vue3 + SpringBoot 2.7）

---

## 📋 目录

1. [高可用系统的核心指标](#1-高可用系统的核心指标)
2. [演进路径概览](#2-演进路径概览)
3. [阶段一：单体应用优化](#3-阶段一单体应用优化)
4. [阶段二：应用层高可用](#4-阶段二应用层高可用)
5. [阶段三：数据层高可用](#5-阶段三数据层高可用)
6. [阶段四：服务拆分与微服务](#6-阶段四服务拆分与微服务)
7. [阶段五：全面高可用架构](#7-阶段五全面高可用架构)
8. [监控与运维](#8-监控与运维)
9. [技术选型建议](#9-技术选型建议)

---

## 1. 高可用系统的核心指标

### 1.1 可用性指标

```
99.9% 可用性 = 年停机时间 8.76 小时
99.99% 可用性 = 年停机时间 52.56 分钟
99.999% 可用性 = 年停机时间 5.26 分钟
```

### 1.2 关键考虑因素

#### 1.2.1 前端（Vue3）考虑点

- **CDN加速**：静态资源分发
- **多环境部署**：开发/测试/生产环境隔离
- **错误监控**：Sentry、监控宝等
- **降级策略**：服务不可用时的友好提示
- **缓存策略**：浏览器缓存、Service Worker
- **负载均衡**：Nginx反向代理多个前端实例

#### 1.2.2 后端（SpringBoot 2.7）考虑点

- **无状态设计**：服务实例可水平扩展
- **健康检查**：Actuator健康端点
- **熔断降级**：Sentinel/Hystrix
- **限流保护**：防止系统过载
- **数据一致性**：分布式事务处理
- **缓存策略**：Redis集群、本地缓存

#### 1.2.3 基础设施考虑点

- **负载均衡**：Nginx、F5、云负载均衡器
- **服务发现**：Nacos、Consul、Eureka
- **配置中心**：Nacos Config、Apollo
- **消息队列**：RabbitMQ、RocketMQ、Kafka
- **数据库**：主从复制、读写分离、分库分表
- **监控告警**：Prometheus + Grafana

---

## 2. 演进路径概览

```
阶段一：单体应用优化
  ↓
阶段二：应用层高可用（多实例 + 负载均衡）
  ↓
阶段三：数据层高可用（主从复制 + 读写分离）
  ↓
阶段四：服务拆分（微服务化）
  ↓
阶段五：全面高可用（服务治理 + 分布式架构）
```

---

## 3. 阶段一：单体应用优化

### 3.1 目标
- 提升单实例性能
- 建立监控体系
- 优化代码质量

### 3.2 前端优化（Vue3）

#### 3.2.1 代码分割与懒加载

```typescript
// router/index.ts
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  {
    path: '/user',
    name: 'User',
    // 路由懒加载，减少首屏加载时间
    component: () => import('@/views/User/index.vue')
  },
  {
    path: '/order',
    name: 'Order',
    component: () => import('@/views/Order/index.vue')
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

export default router
```

#### 3.2.2 组件懒加载

```vue
<!-- 大组件使用异步加载 -->
<script setup lang="ts">
import { defineAsyncComponent } from 'vue'

// 异步加载大组件
const HeavyComponent = defineAsyncComponent(() => 
  import('@/components/HeavyComponent.vue')
)
</script>

<template>
  <HeavyComponent v-if="showHeavy" />
</template>
```

#### 3.2.3 请求优化

```typescript
// utils/request.ts
import axios from 'axios'
import { ElMessage } from 'element-plus'

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 10000,
  // 请求去重，防止重复请求
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求拦截器
request.interceptors.request.use(
  config => {
    // 添加token
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  error => Promise.reject(error)
)

// 响应拦截器
request.interceptors.response.use(
  response => response.data,
  error => {
    // 统一错误处理
    if (error.response?.status === 401) {
      // 跳转登录
      window.location.href = '/login'
    }
    ElMessage.error(error.message || '请求失败')
    return Promise.reject(error)
  }
)

export default request
```

#### 3.2.4 错误监控

```typescript
// utils/errorHandler.ts
import * as Sentry from '@sentry/vue'

export function initErrorMonitoring() {
  Sentry.init({
    app,
    dsn: 'your-sentry-dsn',
    integrations: [
      new Sentry.BrowserTracing(),
      new Sentry.Replay()
    ],
    tracesSampleRate: 1.0,
    replaysSessionSampleRate: 0.1,
    replaysOnErrorSampleRate: 1.0
  })
}

// 全局错误捕获
app.config.errorHandler = (err, instance, info) => {
  Sentry.captureException(err)
  console.error('Global error:', err, info)
}
```

### 3.3 后端优化（SpringBoot 2.7）

#### 3.3.1 连接池优化

```yaml
# application.yml
spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-test-query: SELECT 1
```

#### 3.3.2 缓存配置

```java
// config/CacheConfig.java
@Configuration
@EnableCaching
public class CacheConfig {
    
    @Bean
    public CacheManager cacheManager() {
        RedisCacheManager.Builder builder = RedisCacheManager
            .RedisCacheManagerBuilder
            .fromConnectionFactory(redisConnectionFactory())
            .cacheDefaults(cacheConfiguration());
        return builder.build();
    }
    
    private RedisCacheConfiguration cacheConfiguration() {
        return RedisCacheConfiguration.defaultCacheConfig()
            .entryTtl(Duration.ofMinutes(10))
            .serializeKeysWith(RedisSerializationContext.SerializationPair
                .fromSerializer(new StringRedisSerializer()))
            .serializeValuesWith(RedisSerializationContext.SerializationPair
                .fromSerializer(new GenericJackson2JsonRedisSerializer()));
    }
}
```

#### 3.3.3 健康检查

```yaml
# application.yml
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
```

```java
// controller/HealthController.java
@RestController
@RequestMapping("/actuator")
public class HealthController {
    
    @Autowired
    private HealthIndicator healthIndicator;
    
    @GetMapping("/health")
    public Map<String, Object> health() {
        Health health = healthIndicator.health();
        return Map.of(
            "status", health.getStatus(),
            "details", health.getDetails()
        );
    }
}
```

#### 3.3.4 日志配置

```xml
<!-- logback-spring.xml -->
<configuration>
    <springProfile name="prod">
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>logs/application.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>logs/application.%d{yyyy-MM-dd}.log</fileNamePattern>
                <maxHistory>30</maxHistory>
            </rollingPolicy>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="FILE" />
        </root>
    </springProfile>
</configuration>
```

### 3.4 数据库优化

```sql
-- 索引优化
CREATE INDEX idx_user_phone ON user(phone);
CREATE INDEX idx_order_create_time ON order(create_time);

-- 查询优化：避免SELECT *
SELECT id, name, phone FROM user WHERE phone = 'xxx';

-- 分页优化
SELECT * FROM order 
WHERE create_time > '2024-01-01' 
ORDER BY create_time DESC 
LIMIT 20 OFFSET 0;
```

---

## 4. 阶段二：应用层高可用

### 4.1 目标
- 多实例部署
- 负载均衡
- 服务无状态化

### 4.2 架构图

```
                    ┌─────────────┐
                    │   Nginx      │
                    │ 负载均衡器   │
                    └──────┬───────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────▼────┐       ┌─────▼─────┐      ┌─────▼─────┐
   │ 实例1   │       │  实例2    │      │  实例3    │
   │ :8080   │       │  :8081    │      │  :8082    │
   └────┬────┘       └─────┬─────┘      └─────┬─────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
                    ┌──────▼───────┐
                    │   Redis      │
                    │  (Session)   │
                    └──────────────┘
                           │
                    ┌──────▼───────┐
                    │   MySQL      │
                    │  (单实例)    │
                    └──────────────┘
```

### 4.3 Nginx配置

```nginx
# nginx.conf
upstream backend {
    # 负载均衡策略：轮询
    server 192.168.1.10:8080 weight=1;
    server 192.168.1.11:8080 weight=1;
    server 192.168.1.12:8080 weight=1;
    
    # 健康检查
    keepalive 32;
}

server {
    listen 80;
    server_name api.example.com;
    
    # 前端静态资源
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
    
    # 后端API代理
    location /api {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        
        # 超时设置
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }
    
    # 健康检查端点
    location /health {
        access_log off;
        proxy_pass http://backend/actuator/health;
    }
}
```

### 4.4 SpringBoot Session共享

```xml
<!-- pom.xml -->
<dependency>
    <groupId>org.springframework.session</groupId>
    <artifactId>spring-session-data-redis</artifactId>
</dependency>
```

```java
// config/SessionConfig.java
@Configuration
@EnableRedisHttpSession(maxInactiveIntervalInSeconds = 1800)
public class SessionConfig {
    // Session存储在Redis，实现多实例共享
}
```

### 4.5 Docker部署

```dockerfile
# Dockerfile
FROM openjdk:8-jre-alpine
WORKDIR /app
COPY target/app.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
```

```yaml
# docker-compose.yml
version: '3.8'
services:
  app1:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SERVER_PORT=8080
    networks:
      - app-network
      
  app2:
    build: .
    ports:
      - "8081:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SERVER_PORT=8080
    networks:
      - app-network
      
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    networks:
      - app-network
    depends_on:
      - app1
      - app2

networks:
  app-network:
    driver: bridge
```

### 4.6 前端CDN部署

```typescript
// vite.config.ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  build: {
    // 代码分割
    rollupOptions: {
      output: {
        manualChunks: {
          'vue-vendor': ['vue', 'vue-router', 'pinia'],
          'element-plus': ['element-plus'],
          'utils': ['axios', 'dayjs']
        }
      }
    },
    // 资源压缩
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true,
        drop_debugger: true
      }
    }
  },
  // CDN配置
  base: process.env.NODE_ENV === 'production' 
    ? 'https://cdn.example.com/' 
    : '/'
})
```

---

## 5. 阶段三：数据层高可用

### 5.1 目标
- 数据库主从复制
- 读写分离
- 数据备份

### 5.2 架构图

```
                    ┌─────────────┐
                    │   Nginx      │
                    └──────┬───────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────▼────┐       ┌─────▼─────┐      ┌─────▼─────┐
   │ 实例1   │       │  实例2    │      │  实例3    │
   └────┬────┘       └─────┬─────┘      └─────┬─────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────▼────┐       ┌─────▼─────┐      ┌─────▼─────┐
   │ MySQL   │       │  MySQL    │      │  MySQL    │
   │ Master  │──────▶│  Slave1   │      │  Slave2   │
   │ (写)    │ 复制  │  (读)     │      │  (读)     │
   └─────────┘       └───────────┘      └───────────┘
```

### 5.3 MySQL主从配置

#### 5.3.1 Master配置

```ini
# /etc/mysql/my.cnf (Master)
[mysqld]
server-id = 1
log-bin = mysql-bin
binlog-format = ROW
```

```sql
-- Master上执行
CREATE USER 'repl'@'%' IDENTIFIED BY 'password';
GRANT REPLICATION SLAVE ON *.* TO 'repl'@'%';
FLUSH PRIVILEGES;

SHOW MASTER STATUS;
-- 记录 File 和 Position
```

#### 5.3.2 Slave配置

```ini
# /etc/mysql/my.cnf (Slave)
[mysqld]
server-id = 2
relay-log = mysql-relay-bin
read-only = 1
```

```sql
-- Slave上执行
CHANGE MASTER TO
  MASTER_HOST='192.168.1.10',
  MASTER_USER='repl',
  MASTER_PASSWORD='password',
  MASTER_LOG_FILE='mysql-bin.000001',
  MASTER_LOG_POS=154;

START SLAVE;
SHOW SLAVE STATUS\G
```

### 5.4 SpringBoot读写分离

```xml
<!-- pom.xml -->
<dependency>
    <groupId>com.alibaba</groupId>
    <artifactId>druid-spring-boot-starter</artifactId>
    <version>1.2.16</version>
</dependency>
```

```yaml
# application.yml
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      master:
        url: jdbc:mysql://192.168.1.10:3306/db?useUnicode=true&characterEncoding=utf8
        username: root
        password: password
        driver-class-name: com.mysql.cj.jdbc.Driver
      slave:
        url: jdbc:mysql://192.168.1.11:3306/db?useUnicode=true&characterEncoding=utf8
        username: root
        password: password
        driver-class-name: com.mysql.cj.jdbc.Driver
```

```java
// config/DataSourceConfig.java
@Configuration
public class DataSourceConfig {
    
    @Bean
    @Primary
    public DataSource masterDataSource() {
        DruidDataSource dataSource = new DruidDataSource();
        // Master配置
        return dataSource;
    }
    
    @Bean
    public DataSource slaveDataSource() {
        DruidDataSource dataSource = new DruidDataSource();
        // Slave配置
        return dataSource;
    }
    
    @Bean
    public DataSource routingDataSource() {
        DynamicRoutingDataSource routingDataSource = new DynamicRoutingDataSource();
        Map<Object, Object> dataSourceMap = new HashMap<>();
        dataSourceMap.put("master", masterDataSource());
        dataSourceMap.put("slave", slaveDataSource());
        routingDataSource.setTargetDataSources(dataSourceMap);
        routingDataSource.setDefaultTargetDataSource(masterDataSource());
        return routingDataSource;
    }
}
```

```java
// config/DynamicDataSource.java
public class DynamicDataSource extends AbstractRoutingDataSource {
    @Override
    protected Object determineCurrentLookupKey() {
        return DynamicDataSourceContextHolder.getDataSourceType();
    }
}

// 使用AOP切换数据源
@Aspect
@Component
public class DataSourceAspect {
    
    @Around("@annotation(ReadOnly)")
    public Object switchDataSource(ProceedingJoinPoint point) throws Throwable {
        DynamicDataSourceContextHolder.setDataSourceType("slave");
        try {
            return point.proceed();
        } finally {
            DynamicDataSourceContextHolder.clearDataSourceType();
        }
    }
}

// 使用示例
@Service
public class UserService {
    
    @Autowired
    private UserMapper userMapper;
    
    // 写操作，使用Master
    public void saveUser(User user) {
        userMapper.insert(user);
    }
    
    // 读操作，使用Slave
    @ReadOnly
    public User getUserById(Long id) {
        return userMapper.selectById(id);
    }
}
```

### 5.5 Redis高可用

#### 5.5.1 Redis主从配置

```conf
# redis-master.conf
port 6379
bind 0.0.0.0
requirepass masterpassword
```

```conf
# redis-slave.conf
port 6380
bind 0.0.0.0
replicaof 192.168.1.10 6379
masterauth masterpassword
```

#### 5.5.2 Redis Sentinel（哨兵）

```conf
# sentinel.conf
port 26379
sentinel monitor mymaster 192.168.1.10 6379 2
sentinel auth-pass mymaster masterpassword
sentinel down-after-milliseconds mymaster 5000
sentinel failover-timeout mymaster 10000
```

```java
// config/RedisConfig.java
@Configuration
public class RedisConfig {
    
    @Bean
    public LettuceConnectionFactory redisConnectionFactory() {
        RedisSentinelConfiguration sentinelConfig = 
            new RedisSentinelConfiguration()
                .master("mymaster")
                .sentinel("192.168.1.10", 26379)
                .sentinel("192.168.1.11", 26379)
                .sentinel("192.168.1.12", 26379);
        
        return new LettuceConnectionFactory(sentinelConfig);
    }
}
```

---

## 6. 阶段四：服务拆分与微服务

### 6.1 目标
- 按业务域拆分服务
- 服务独立部署
- 服务间通信

### 6.2 架构图

```
                    ┌─────────────┐
                    │   Gateway   │
                    │  (网关)     │
                    └──────┬───────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────▼────┐       ┌─────▼─────┐      ┌─────▼─────┐
   │ 用户服务 │       │  订单服务  │      │  支付服务  │
   │ :8001   │       │  :8002    │      │  :8003    │
   └────┬────┘       └─────┬─────┘      └─────┬─────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
                    ┌──────▼───────┐
                    │   Nacos      │
                    │  (注册中心)  │
                    └──────────────┘
```

### 6.3 服务拆分原则

1. **按业务域拆分**：用户服务、订单服务、支付服务
2. **数据库独立**：每个服务有自己的数据库
3. **API版本管理**：/api/v1/user, /api/v2/user
4. **服务间通信**：RESTful API、消息队列

### 6.4 Spring Cloud Gateway

```xml
<!-- pom.xml -->
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-gateway</artifactId>
</dependency>
<dependency>
    <groupId>com.alibaba.cloud</groupId>
    <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
</dependency>
```

```yaml
# application.yml
spring:
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/api/user/**
          filters:
            - StripPrefix=2
        - id: order-service
          uri: lb://order-service
          predicates:
            - Path=/api/order/**
          filters:
            - StripPrefix=2
    nacos:
      discovery:
        server-addr: 192.168.1.10:8848
```

### 6.5 服务注册与发现

```java
// UserServiceApplication.java
@SpringBootApplication
@EnableDiscoveryClient
public class UserServiceApplication {
    public static void main(String[] args) {
        SpringApplication.run(UserServiceApplication.class, args);
    }
}
```

```yaml
# user-service/application.yml
spring:
  application:
    name: user-service
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.10:8848
server:
  port: 8001
```

### 6.6 服务间调用（OpenFeign）

```xml
<!-- pom.xml -->
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-openfeign</artifactId>
</dependency>
```

```java
// service/OrderService.java
@FeignClient(name = "user-service", path = "/api/user")
public interface UserServiceClient {
    
    @GetMapping("/{id}")
    Result<User> getUserById(@PathVariable Long id);
    
    @PostMapping("/batch")
    Result<List<User>> getUsersByIds(@RequestBody List<Long> ids);
}
```

```java
// controller/OrderController.java
@RestController
@RequestMapping("/api/order")
public class OrderController {
    
    @Autowired
    private UserServiceClient userServiceClient;
    
    @GetMapping("/{id}")
    public Result<Order> getOrder(@PathVariable Long id) {
        Order order = orderService.getById(id);
        // 调用用户服务获取用户信息
        Result<User> userResult = userServiceClient.getUserById(order.getUserId());
        order.setUser(userResult.getData());
        return Result.success(order);
    }
}
```

### 6.7 熔断降级（Sentinel）

```xml
<!-- pom.xml -->
<dependency>
    <groupId>com.alibaba.cloud</groupId>
    <artifactId>spring-cloud-starter-alibaba-sentinel</artifactId>
</dependency>
```

```yaml
# application.yml
spring:
  cloud:
    sentinel:
      transport:
        dashboard: 192.168.1.10:8080
      datasource:
        flow:
          nacos:
            server-addr: 192.168.1.10:8848
            dataId: ${spring.application.name}-flow-rules
            groupId: SENTINEL_GROUP
            rule-type: flow
```

```java
// service/OrderService.java
@Service
public class OrderService {
    
    @SentinelResource(
        value = "createOrder",
        fallback = "createOrderFallback",
        blockHandler = "createOrderBlockHandler"
    )
    public Order createOrder(OrderDTO dto) {
        // 业务逻辑
        return order;
    }
    
    // 降级方法
    public Order createOrderFallback(OrderDTO dto, Throwable e) {
        log.error("创建订单失败，降级处理", e);
        return new Order(); // 返回默认值
    }
    
    // 限流方法
    public Order createOrderBlockHandler(OrderDTO dto, BlockException e) {
        throw new BusinessException("系统繁忙，请稍后重试");
    }
}
```

---

## 7. 阶段五：全面高可用架构

### 7.1 完整架构图

```
                    ┌─────────────┐
                    │   CDN       │
                    │  (静态资源) │
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │   Nginx      │
                    │  (负载均衡)  │
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │   Gateway    │
                    │  (API网关)   │
                    └──────┬───────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────▼────┐       ┌─────▼─────┐      ┌─────▼─────┐
   │ 用户服务 │       │  订单服务  │      │  支付服务  │
   │ (多实例) │       │  (多实例) │      │  (多实例) │
   └────┬────┘       └─────┬─────┘      └─────┬─────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────▼────┐       ┌─────▼─────┐      ┌─────▼─────┐
   │  MySQL  │       │  Redis    │      │  RabbitMQ │
   │ 集群    │       │  集群     │      │  (消息)   │
   └─────────┘       └───────────┘      └───────────┘
```

### 7.2 消息队列（RabbitMQ）

```xml
<!-- pom.xml -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-amqp</artifactId>
</dependency>
```

```yaml
# application.yml
spring:
  rabbitmq:
    host: 192.168.1.10
    port: 5672
    username: admin
    password: admin
    virtual-host: /
    # 高可用配置
    publisher-confirms: true
    publisher-returns: true
    template:
      mandatory: true
```

```java
// config/RabbitMQConfig.java
@Configuration
public class RabbitMQConfig {
    
    // 订单队列
    @Bean
    public Queue orderQueue() {
        return QueueBuilder.durable("order.queue").build();
    }
    
    // 订单交换机
    @Bean
    public Exchange orderExchange() {
        return ExchangeBuilder.topicExchange("order.exchange").durable(true).build();
    }
    
    // 绑定
    @Bean
    public Binding orderBinding() {
        return BindingBuilder
            .bind(orderQueue())
            .to(orderExchange())
            .with("order.*")
            .noargs();
    }
}
```

```java
// service/OrderService.java
@Service
public class OrderService {
    
    @Autowired
    private RabbitTemplate rabbitTemplate;
    
    public void createOrder(OrderDTO dto) {
        // 创建订单
        Order order = new Order();
        orderService.save(order);
        
        // 发送消息到队列
        rabbitTemplate.convertAndSend(
            "order.exchange",
            "order.created",
            order
        );
    }
}
```

```java
// listener/OrderListener.java
@Component
@RabbitListener(queues = "order.queue")
public class OrderListener {
    
    @RabbitHandler
    public void handleOrderCreated(Order order) {
        // 处理订单创建后的业务逻辑
        // 如：发送通知、更新库存等
        log.info("收到订单创建消息: {}", order.getId());
    }
}
```

### 7.3 分布式事务（Seata）

```xml
<!-- pom.xml -->
<dependency>
    <groupId>com.alibaba.cloud</groupId>
    <artifactId>spring-cloud-starter-alibaba-seata</artifactId>
</dependency>
```

```yaml
# application.yml
seata:
  enabled: true
  application-id: ${spring.application.name}
  tx-service-group: my_test_tx_group
  config:
    type: nacos
    nacos:
      server-addr: 192.168.1.10:8848
      group: SEATA_GROUP
  registry:
    type: nacos
    nacos:
      server-addr: 192.168.1.10:8848
      group: SEATA_GROUP
```

```java
// service/OrderService.java
@Service
public class OrderService {
    
    @GlobalTransactional(rollbackFor = Exception.class)
    public void createOrder(OrderDTO dto) {
        // 1. 创建订单
        orderMapper.insert(order);
        
        // 2. 扣减库存（调用库存服务）
        inventoryService.reduceStock(dto.getProductId(), dto.getQuantity());
        
        // 3. 扣减余额（调用账户服务）
        accountService.deductBalance(dto.getUserId(), dto.getAmount());
        
        // 如果任何一步失败，全部回滚
    }
}
```

### 7.4 限流与降级

```java
// config/SentinelConfig.java
@Configuration
public class SentinelConfig {
    
    @PostConstruct
    public void initRules() {
        // 限流规则
        List<FlowRule> rules = new ArrayList<>();
        FlowRule rule = new FlowRule();
        rule.setResource("createOrder");
        rule.setGrade(RuleConstant.FLOW_GRADE_QPS);
        rule.setCount(100); // 每秒100个请求
        rules.add(rule);
        FlowRuleManager.loadRules(rules);
        
        // 降级规则
        List<DegradeRule> degradeRules = new ArrayList<>();
        DegradeRule degradeRule = new DegradeRule();
        degradeRule.setResource("createOrder");
        degradeRule.setGrade(RuleConstant.DEGRADE_GRADE_RT);
        degradeRule.setCount(1000); // 响应时间超过1秒
        degradeRule.setTimeWindow(10); // 降级10秒
        degradeRules.add(degradeRule);
        DegradeRuleManager.loadRules(degradeRules);
    }
}
```

---

## 8. 监控与运维

### 8.1 监控体系

#### 8.1.1 Prometheus + Grafana

```yaml
# prometheus.yml
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['192.168.1.10:8080', '192.168.1.11:8080']
```

```xml
<!-- pom.xml -->
<dependency>
    <groupId>io.micrometer</groupId>
    <artifactId>micrometer-registry-prometheus</artifactId>
</dependency>
```

```yaml
# application.yml
management:
  endpoints:
    web:
      exposure:
        include: prometheus,health,metrics
  metrics:
    export:
      prometheus:
        enabled: true
```

#### 8.1.2 日志聚合（ELK）

```yaml
# docker-compose.yml
version: '3.8'
services:
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:7.17.0
    environment:
      - discovery.type=single-node
    ports:
      - "9200:9200"
      
  logstash:
    image: docker.elastic.co/logstash/logstash:7.17.0
    volumes:
      - ./logstash.conf:/usr/share/logstash/pipeline/logstash.conf
    ports:
      - "5044:5044"
      
  kibana:
    image: docker.elastic.co/kibana/kibana:7.17.0
    ports:
      - "5601:5601"
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
```

### 8.2 告警配置

```yaml
# alertmanager.yml
global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 12h
  receiver: 'web.hook'

receivers:
  - name: 'web.hook'
    webhook_configs:
      - url: 'http://192.168.1.10:9093/alert'
```

### 8.3 健康检查与自动恢复

```yaml
# docker-compose.yml
services:
  app:
    image: app:latest
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
```

---

## 9. 技术选型建议

### 9.1 前端技术栈

| 组件 | 推荐方案 | 说明 |
|------|---------|------|
| 框架 | Vue 3 | 组合式API，性能优秀 |
| 构建工具 | Vite | 快速构建，开发体验好 |
| UI组件库 | Element Plus | 成熟稳定，组件丰富 |
| 状态管理 | Pinia | Vue3官方推荐 |
| 路由 | Vue Router | 官方路由方案 |
| HTTP客户端 | Axios | 功能完善，拦截器强大 |
| 错误监控 | Sentry | 功能强大，支持SourceMap |
| CDN | 阿里云/腾讯云 | 国内访问速度快 |

### 9.2 后端技术栈

| 组件 | 推荐方案 | 说明 |
|------|---------|------|
| 框架 | Spring Boot 2.7 | 稳定成熟 |
| 微服务框架 | Spring Cloud Alibaba | 阿里开源，文档完善 |
| 服务注册 | Nacos | 功能强大，支持配置中心 |
| 网关 | Spring Cloud Gateway | 性能好，支持WebFlux |
| 熔断降级 | Sentinel | 功能完善，控制台友好 |
| 分布式事务 | Seata | 阿里开源，支持多种模式 |
| 消息队列 | RabbitMQ/RocketMQ | RabbitMQ轻量，RocketMQ高性能 |
| 缓存 | Redis | 单线程模型，性能优秀 |
| 数据库 | MySQL 8.0 | 主从复制，读写分离 |

### 9.3 基础设施

| 组件 | 推荐方案 | 说明 |
|------|---------|------|
| 负载均衡 | Nginx | 反向代理，负载均衡 |
| 容器化 | Docker | 应用容器化 |
| 编排 | Kubernetes | 生产环境推荐 |
| 监控 | Prometheus + Grafana | 指标监控 |
| 日志 | ELK Stack | 日志聚合分析 |
| CI/CD | Jenkins/GitLab CI | 自动化部署 |

---

## 10. 演进时间表建议

### 第一阶段（1-2个月）：单体优化
- ✅ 代码优化、性能调优
- ✅ 建立监控体系
- ✅ 完善日志系统

### 第二阶段（2-3个月）：应用层高可用
- ✅ 多实例部署
- ✅ Nginx负载均衡
- ✅ Session共享

### 第三阶段（3-4个月）：数据层高可用
- ✅ MySQL主从复制
- ✅ 读写分离
- ✅ Redis高可用

### 第四阶段（4-6个月）：服务拆分
- ✅ 按业务域拆分服务
- ✅ 服务注册与发现
- ✅ API网关

### 第五阶段（6-12个月）：全面高可用
- ✅ 消息队列
- ✅ 分布式事务
- ✅ 完善监控告警

---

## 11. 关键注意事项

### 11.1 无状态设计
- ✅ Session存储在Redis
- ✅ 文件上传使用OSS
- ✅ 不依赖本地文件系统

### 11.2 幂等性设计
```java
// 接口幂等性保证
@PostMapping("/create")
public Result create(@RequestBody OrderDTO dto) {
    // 使用唯一标识防止重复提交
    String idempotentKey = dto.getIdempotentKey();
    if (redisTemplate.hasKey("order:create:" + idempotentKey)) {
        return Result.error("请勿重复提交");
    }
    redisTemplate.opsForValue().set("order:create:" + idempotentKey, "1", 5, TimeUnit.MINUTES);
    
    // 业务逻辑
    return Result.success();
}
```

### 11.3 优雅降级
```typescript
// 前端降级策略
async function fetchData() {
  try {
    return await api.getData();
  } catch (error) {
    // 降级：使用缓存数据
    const cachedData = localStorage.getItem('cachedData');
    if (cachedData) {
      return JSON.parse(cachedData);
    }
    // 降级：显示友好提示
    ElMessage.warning('服务暂时不可用，请稍后重试');
    throw error;
  }
}
```

### 11.4 数据一致性
- **最终一致性**：通过消息队列保证
- **强一致性**：使用分布式事务（Seata）
- **补偿机制**：失败后自动重试或人工介入

---

## 总结

高可用系统的建设是一个**渐进式**的过程，需要根据业务发展逐步演进：

1. **先优化单体**：提升单实例性能和稳定性
2. **再横向扩展**：多实例 + 负载均衡
3. **然后数据高可用**：主从复制、读写分离
4. **接着服务拆分**：微服务化，独立部署
5. **最后全面高可用**：消息队列、分布式事务、完善监控

**关键原则**：
- ✅ 无状态设计
- ✅ 幂等性保证
- ✅ 优雅降级
- ✅ 监控告警
- ✅ 自动化运维

记住：**高可用不是一蹴而就的，需要持续优化和改进！**

